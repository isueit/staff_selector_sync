staff_profile_sync.admin_settings_form:
  path: '/admin/config/content/ISUEO/staff_profile_sync'
  defaults:
    _form: '\Drupal\staff_profile_sync\Form\SettingsForm'
    _title: 'Staff Profile Sync'
  requirements:
    _permission: 'access administration pages'
    _permission: 'administer staff profile sync'
  options:
    _admin-route: TRUE
